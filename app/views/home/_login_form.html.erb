<%= form_tag("signin", method: "post") do %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
 <%= text_field_tag :username, nil, placeholder: 'Email / Username' %>
 <%= submit_tag 'Sign up / in' %>
<% end %>
